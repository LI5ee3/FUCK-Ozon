# starlette >= 1.0.1 修复 CVE-2026-48710（Host 头导致 request.url.path 与路由 path 不一致）
# starlette >= 0.49.1 修复 CVE-2025-62727（FileResponse multi-Range O(n²) CPU 耗尽）
fastapi==0.141.1
starlette==1.6.0
uvicorn==0.35.0
openpyxl==3.1.5
