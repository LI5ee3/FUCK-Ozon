# Ozon 独立拉取验证报告

验证时间：2026-08-20（北京时间）  
订单范围：2026-05-20 00:00 至验证时刻（北京时间）

## 凭据与能力

- `.env` 权限为 `600`，测试过程未输出 Client ID 或 API Key。
- 店铺1：Willvast Store，结算币种 USD，`/v1/roles` 与 `/v1/seller/info` 均为 HTTP 200，角色 Admin。
- 店铺2：Willgo Store，结算币种 CNY，`/v1/roles` 与 `/v1/seller/info` 均为 HTTP 200，角色 Admin。

## 独立拉取结果

| 模块 | 店铺1 | 店铺2 | 只变化的数据表 | 错误联动 |
|---|---:|---:|---|---|
| 订单 | 3,904 | 5,858 | `orders`、`order_items`、`order_api_records` | 无 |
| 财务 | 17,690 | 25,648 | `finance_records` | 无 |
| 退货 | 18 | 31 | `return_records` | 无 |
| Premium分析 | 29,025 | 28,575 | `analytics_records` | 无 |
| 库存 | 112 | 215 | `stock_snapshots` | 无 |
| 价格 | 112 | 215 | `price_snapshots` | 无 |
| 买家问答 | 362 | 366 | `question_records` | 无 |

财务接口单次最多一个月，实际按不超过30天分段并按 `operation_id` 去重。API共返回店铺1 18,208行、店铺2 26,337行，去重后分别保存17,690和25,648条。

库存和价格接口只返回当前快照，不提供历史日期范围。本次已保存当前快照，后续每次独立拉取会形成历史。

Premium接口出现过 HTTP 429；共享客户端按限流退避后成功完成，没有调用其他模块。

## 店铺1 API 与 CSV 对比

| 项目 | FBP | realFBS | WHD | 合计 |
|---|---:|---:|---:|---:|
| CSV不同订单号 | 2,832 | 882 | 136 | 3,850 |
| API不同订单号 | 2,851 | 915 | 138 | 3,904 |

- CSV的3,850个订单号全部在API中找到；CSV独有为0，API新增54个。
- 渠道：3,850 / 3,850 完全一致。
- SKU集合：3,850 / 3,850 完全一致。
- 商品数量：3,850 / 3,850 完全一致。
- 创建时间（精确到秒）：3,850 / 3,850 完全一致，证明CSV无时区重复换算。
- FBP固定取消原因：270 / 270 完全一致。
- 状态：3,802 / 3,850 一致；48个差异均为CSV导出后状态继续推进：运输中→已签收25、待取件→运输中9、等待发运→运输中7、待备货→运输中3、待取件→已签收4。

API更新与CSV补充合并后的有效统计：

| 渠道 | 有效订单数 | 有效货件数 |
|---|---:|---:|
| FBP | 2,715 | 2,749 |
| realFBS | 844 | 858 |
| WHD | 125 | 125 |

该结果高于CSV基线是因为API比CSV晚拉取并新增54个订单。WHD比CSV有效订单多2个。

## 马帮成本

- 500条成本全部仍能匹配API订单号。
- 成本合计仍为 ¥351,164.6576。
- 订单API重拉后成本表行数和总额均未变化。
- 未进行SKU成本分摊。

## 测试中发现并修复

1. FBS商品价格是字符串，FBO/WHD价格是 `{amount, currency}` 对象；已统一兼容并保留实际币种。
2. FBO/WHD取消数据没有 `cancelled_after_ship`。API空值曾覆盖CSV已有发货证据；已改为保留CSV补充值。重拉后12条WHD发货后取消仍完整保留。
3. 同步中心已验证七个独立按钮、近三个月日期、中文状态和成功记录；页面控制台无错误。

自动回归：3项通过，包括真实四文件基线、两种API价格结构、库存同步表隔离。
